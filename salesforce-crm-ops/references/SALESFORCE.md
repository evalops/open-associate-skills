# Salesforce REST references (concepts)

- OAuth token endpoint: /services/oauth2/token
- REST Query resource: /services/data/vXX.X/query/?q=<SOQL>
- Objects: Lead, Account, Opportunity, Task, Event

Adapt to your org’s custom objects and required fields.
